name: {{project_name}}
root: {{project_path}}

windows:
   - editor:
       layout: main-vertical
       panes:
         - source .env && source .venv/bin/activate && nvim
         - source .env && source .venv/bin/activate && tree .
